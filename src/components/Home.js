const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<>
			<div className="container">

				<h2>weatherstew</h2>
			</div>
		</>
	)
}

export default Home
